export enum DividerVariants {
    SOLID='solid',
    DOTTED='dotted',
}

export enum TagVariants {
    SOLID='solid',
    LINE='line',
}

export enum IconSizes {
    SMALL='small',
    MEDIUM='medium',
    LARGE='large',
}